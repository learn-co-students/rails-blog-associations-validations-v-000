<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
  <% if @post.tags.size > 0 %>
    <% @post.tags.each do |t| %>
      <%= t.name %>
    <%end%>
  <%end%>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
