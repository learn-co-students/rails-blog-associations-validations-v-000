<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %> <%= @post.tags.name %>
  <%= @post.content %>
</p>
   <h3>Content:</h3>
   <p><%= @post.content %></p>
    <% @post.tags.each do |tag| %>
       <h3>Tags: <%= tag.name %> </h3>
       <% end %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
