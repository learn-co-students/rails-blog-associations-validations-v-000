<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %><br>
  <h4>Tags: </h4>
  <% @post.tags.each do |t| %>
    <%=t.name%>
  <%end%>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
