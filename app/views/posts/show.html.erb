<p id="notice"><%= notice %></p>

<p>
  <h3>Name: <%= @post.name %></h3>
  <h2>Tags: </h2>
           <% @post.tags.each do |x|%>
      <li>    <%= x.name %> </li>
            <%end%>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
