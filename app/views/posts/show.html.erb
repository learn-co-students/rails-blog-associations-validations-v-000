<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>

  <h6>Content:</h6>
  <%= @post.content %>

  <h6>Tags:</h6>
    <% @post.tags.each do |t| %>
      <%= t.name %>
    <% end %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
