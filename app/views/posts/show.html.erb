<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %>
</p>

<div>
  <%= @post.content %>
</div>

<h4>Tags:</h4>
<%= @post.tags.each do |tag| %>
  <span><%= tag.name %>&nbsp;</span>
<% end %>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
