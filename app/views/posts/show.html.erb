<p id="notice"><%= notice %></p>

<p>
  <h1>Name:</h1>
  <%= @post.name %>
  <h3>Content:</h3>
  <td><%= @post.content %></td>
</p>

<p>
  <%= @post.tags.each do |tag| %>
    <%= tag.name %>
    <% end %>

</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
