<p id="notice"><%= notice %></p>

<p>
  <h3>Name:</h3>
  <%= @post.name %></br>
  <% Tag.find(@post.tag_ids).each do |tag| %>
    <ul>
      <li><%= tag.name %></li>
    </ul>
  <% end %>

</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
