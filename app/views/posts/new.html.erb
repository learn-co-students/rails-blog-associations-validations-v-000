<h1>Post title</h1>

<%= form_for @post do |f| %>
  <p><%= f.label :name %>
  <%= f.text_field :name %></p>

  <p><%= f.label :content %>
  <%= f.text_field :content %></p>

  <%= f.submit %>
<% end %>
<%= render 'form' %>

<%= link_to 'Back', posts_path %>
