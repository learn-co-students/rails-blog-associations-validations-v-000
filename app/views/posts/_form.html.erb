<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Name" %><br>
    <%= f.text_field :name %><br>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %><br>
  </div>
  <div class="field">
     <%= f.label "cute" %> <br />
     <%= f.check_box :tags, {}, "adorable", nil %> <br />
 </div>
 <div class="field">
     <%= f.label "adorable" %> <br />
     <%= f.check_box :tags, {}, 'adorable', nil %> <br />
 </div>
  <div class="actions">
    <%= f.submit "Create Post" class: "btn btn-default" %>
  </div>
<% end %>
