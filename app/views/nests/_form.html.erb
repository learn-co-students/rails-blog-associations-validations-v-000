<%= form_for(@nest) do |f| %>
  <% if @nest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nest.errors.count, "error") %> prohibited this nest from being saved:</h2>

      <ul>
      <% @nest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br>
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :fabrication %><br>
    <%= f.text_field :fabrication %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
