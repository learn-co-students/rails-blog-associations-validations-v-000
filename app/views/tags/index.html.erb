<h1>Tags#index</h1>
<% if !@tags.nil? %>
  <table border="1">
    <tbody>
      <% @tags.each do |tag| %>
        <tr>
        <td><%= tag.name %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
<br><a href="/tags/new">Create new tag</a>
