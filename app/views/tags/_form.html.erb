<%= form_for(@tag) do |f| %>
<% if @tag.errors.any? %>
<div id="error_explanations">
  <h2><%= pluralize(@tag.errors.count, "error") %> prohibited this tag</h2>
<ul>
  <% @tag.errors.full_messages.each do |msg| %>
  <li><%= msg %></li>
</ul>
</div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
